decoration {
    rounding = 10

    # Dim inactive windows so the active one stands out
    dim_inactive = true
    dim_strength = 0.1

    # Inactive windows fade back a little
    active_opacity   = 1.0
    inactive_opacity = 1.0

    blur {
        enabled = true
        size = 2
        passes = 1
    }
}
