[Login]
# Lid close, no external monitor → suspend-then-hibernate
HandleLidSwitch=suspend-then-hibernate
# Lid close, on AC power with no external monitor → same
HandleLidSwitchExternalPower=suspend-then-hibernate
# Lid close, external monitor connected → keep running (default, explicit)
HandleLidSwitchDocked=ignore
