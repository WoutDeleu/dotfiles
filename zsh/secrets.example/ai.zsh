# AI / Anthropic credentials — TEMPLATE.
# Real secrets live in ~/.config/secrets/*.zsh (machine-local, never tracked).
# Setup:
#   mkdir -p ~/.config/secrets && chmod 700 ~/.config/secrets
#   cp ~/dotfiles/zsh/secrets.example/ai.zsh ~/.config/secrets/ai.zsh
#   chmod 600 ~/.config/secrets/ai.zsh   # then fill in real values
export ANTHROPIC_BASE_URL=""
export ANTHROPIC_API_KEY=""
